PORT=8000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aapda
JWT_SECRET=change_this_to_a_long_random_string
ML_SERVICE_URL=http://127.0.0.1:8001
# Set to "true" to run the API on in-memory demo data instead of Postgres
# (useful for a quick hackathon demo with zero DB setup).
DEMO_MODE=true
